<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.mappers.WhiskeyMapper">

	<select id="selectByWriter" resultType="String" parameterType="String">
		select * from whiskey
		where writer = #{writer}
	</select>

	<select id="selectDistinctWhiskeyList" resultType="String">
		select distinct menu from whiskey
	</select>

</mapper>