<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTO Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">
	<insert id="registMember">
		INSERT INTO
		member (irum, registNumber, mid, mpw)
		VALUES
		(#{irum}, #{registNumber},
		#{mid}, #{mpw})
	</insert>

	<insert id="findMember">
		SELECT *
		FROM member
		WHERE
		registNumber =
		#{registNumber}
	</insert>

	<insert id="correctMember">
		SELECT *
		FROM member
		WHERE
		mid = #{mid}
		AND
		mpw = #{mpw}
	</insert>
</mapper>